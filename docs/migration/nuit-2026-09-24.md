# Session de nuit du 2026-09-24 — compte rendu (4 lots)

> Prompt unique : `docs/plan-livraison-finale.md` § « Session de nuit du
> 2026-09-24 ». Aucun commit, aucun push, aucune fusion : tout est dans la
> copie de travail (commandes en fin de document). `git fetch` au départ :
> **aucun commit** sur `origin/dev` devant `HEAD` (rien à absorber). Port
> 4321 libre toute la nuit : builds dans le dépôt.

## Bilan en une ligne par lot

| Lot | Statut | Écart principal |
| --- | --- | --- |
| 1 — Revue R3, constats 3 à 8 | **fait** | aucun |
| 2 — L16, statique vs aperçu | **fait** (côté code) | reste 1 étape UI CloudCannon : poser `EDITOR_PREVIEW=1` sur les sites dev et Édition |
| 3 — D18, D19, héros | **fait** | `_snippets` non concerné ; poids ajouté à `dist` : **+37 Mo** (> 15 Mo, rien retiré) ; 1 « héros » restant n'en est pas un |
| 4 — Forme des articles | **fait sauf l'item 2** | snippets CloudCannon impossibles en `.md` pur (aucun gabarit HTML) — décision : `.mdx` ou coller les modèles du guide |

## Lot 1 — Revue R3, constats 3 à 8

**Fait** (galerie `component-library/src/components/galerie/galerie.astro`,
route `src/pages/[lang]/solutions/[slug].astro`, `src/lib/solutions/slug.ts`,
`src/lib/forms/field-name.ts`) :

- **R3-3 libellés de la visionneuse** : « Fermer », « Image précédente /
  suivante », « Agrandir l'image n sur N » suivent la langue de la page (lue
  dans l'URL, même règle que `form.astro` ; repli FR dans l'éditeur).
- **R3-4 `role="dialog"`** : chaque vue agrandie porte `role="dialog"` et un
  nom accessible « Image n sur N : alt » — sans `aria-modal` (le focus n'est
  pas piégé, l'annoncer modale mentirait).
- **R3-5 e2e couplés au contenu** : `tests/e2e/catalogue-fiche.spec.ts`
  réécrit sans aucun slug (première fiche du catalogue ; saut motivé si la
  langue n'en a pas ; « chaque carte mène à une fiche existante ou au Contact
  prérempli » remplace l'assertion « l'anglais n'a pas de fiche »). Nouveau
  `tests/e2e/galerie.spec.ts` (rôle, nom, libellés FR/EN, identifiants).
- **R3-6 id de galerie** : l'identifiant vient du RANG de la section dans la
  page (`galerie-3`, prop `position` posée par `page.astro` comme `h1Taken`)
  ; l'empreinte djb2 reste le repli hors page. Deux galeries identiques ne
  partagent plus leurs ancres.
- **R3-7 repli de langue** : une fiche sans traduction retombe sur le
  CATALOGUE de l'autre langue (plus l'accueil) ; un fichier hors `fr/`/`en/`
  casse le build en nommant le fichier (`langueDeFiche`, 2 tests) au lieu de
  sortir en français.
- **R3-8 `String.fromCharCode`** : la plage des diacritiques s'écrit
  `/[̀-ͯ]/g` dans `field-name.ts` ET `slug.ts` (qui portait des
  caractères combinants LITTÉRAUX invisibles) ; 2 tests.
- Au passage : les tests e2e comptent `main h1` et non `h1` — la barre
  d'outils de dev d'Astro ajoute 4 `<h1>` dans un shadow DOM que Playwright
  traverse (cause d'un rouge intermittent, expliqué dans le test).

**Gate** : lint 0 err (4 avert. préexistants) · vitest 244/244 · type-check 0 ·
build 202 pages · liens 0 cassé · sections 0 · bookshop 43/43 · aperçus
alignés · redirections 175/175 (+ `--dist`) · parité 3 assumées (exit 0) ·
e2e **78 passés + 3 sautés** (2 = pas de galerie EN, 1 préexistant).

## Lot 2 — L16, statique vs aperçu d'édition

**Fait** : `EDITOR_PREVIEW` porte la politique d'aperçu, `STATIC_ONLY` ne
garde que l'adaptateur.

- `scripts/lib/build-mode.mjs` (pure, testée : `src/lib/build-mode.test.ts`,
  4 combinaisons + filtre des redirections) ; `src/lib/editor-preview.ts`
  (lecture unique, expression membre exacte) ; `src/i18n/blog.ts`,
  `src/lib/announce.ts`, `src/lib/schedule.ts` renommés `editorPreview` ;
  `astro.config.mjs` : Bookshop attaché seulement si STATIC_ONLY **et**
  EDITOR_PREVIEW ; EDITOR_PREVIEW sans STATIC_ONLY = avertissement au build.
- Vérifié par deux builds : `STATIC_ONLY=1 EDITOR_PREVIEW=1` → 213 pages,
  **2 marqueurs `bookshop-live`** sur la page service ; `STATIC_ONLY=1` seul
  (= production CloudCannon) → 213 pages, **0 marqueur**, pas de `_worker.js`.
- Docs : tableau des variables par site dans `docs/operations.md` § 6 ;
  `docs/DEPLOYMENT.md` § 3 et § 6 (contrainte réglée) ; `.env.example` ;
  commentaires (`content.config.ts`, `cloudcannon.config.yml`, routes).

**À faire à la main (UI CloudCannon)** : Site Settings → Build → Environment
variables → ajouter `EDITOR_PREVIEW=1` sur le site **dev** et le site
**Édition** (staging) ; **ne rien ajouter sur Production**. Tant que la
variable manque sur un site d'édition, il construit comme la production :
brouillons absents, pas d'édition visuelle (« No live editing connected »).

**Gate** : lint 0 err · vitest 249/249 · type-check 0 · build 202 · liens 0 ·
sections 0 · bookshop 43/43 · aperçus alignés · redirections 175/175 (+
`--dist`) · parité 3 (exit 0) · e2e 78 + 3 sautés.

## Lot 3 — D18, D19 et héros

**D18 (documents)** — fait :

- `docs/migration/correspondance-urls.json` : les 3 `/document/*` passent de
  `temporaires` à `manuel` (301) : pourquoi-gerez-vous-encore-vos-ti →
  `/fr/services/services-ti-geres/`, cybersecurite → SEvOC,
  webinaire-copilot-buzz-impact → `/fr/ressources/copilot-vs-chatgpt/`. La
  campagne `licences-power-platform` et sa définition de formulaire sont
  gardées (gabarit d'hébergement d'un document à formulaire).
- Articles : guide Licences (FR **et EN** → la campagne FR, il n'y a pas de
  campagne EN) ; CTA de tendances-ti → services gérés, libellé « Découvrez
  nos services TI gérés » (l'ancien « Téléchargez le guide » mentait) ;
  copilot-vs-chatgpt : lien retiré, bannière gardée, phrase « la rediffusion
  sera bientôt disponible » ; les 2 liens de une-journee-secops tombent avec
  l'article (D19).
- `scripts/check-internal-links.mjs` : `ALLOW` vidé (commentaire : jamais pour
  faire passer le gate).

**D19 (7 articles)** — fait :

- `draft: true` sur les 13 fichiers (7 FR + 6 EN ; une-journee-secops n'a pas
  d'EN) ; `draft: false` rétro-rempli sur les **49 autres** articles et dans
  `schemas/blog-{fr,en}.md` (CloudCannon n'affiche un champ que si sa clé
  existe).
- 301 : nouvelle rubrique `articles_retires` de `correspondance-urls.json`
  (**31 adresses** : anciennes WordPress, nouvelles `/ressources/…`, plus 5
  anciennes URL alternatives sans « partie-n- » découvertes par
  `check:redirects --dist`) → `scripts/build-redirects.mjs` les marque
  `retire: true` ; `astro.config.mjs` ne les émet **pas** en aperçu d'édition
  (`filtreRedirectionsSelonMode`, testée). Matrice : **188 règles**, 188/188
  cibles présentes dans `dist`.
- `cloudcannon.config.yml` : métadonnée de carte (clé `draft` affichée telle
  quelle — les gabarits de carte n'ont pas de conditionnel) + `sort_options`
  « Brouillons d'abord / Date / Titre ». Vérifié avec `@cloudcannon/reader` :
  config valide, 13 brouillons reconnus, tri et métadonnée présents.
  **À vérifier dans l'UI** : le rendu de `true`/`false` sur la carte.
- Test : `tests/e2e/brouillons.spec.ts` (lit les brouillons dans
  `src/content/blog` ; 404, absents des listes, aucune page publiée ne les
  lie — articles liés, derniers articles, hreflang).
- Outils de parité : `articles_retires` compte comme « hors comparaison »
  dans `check-parite-texte.py` (sinon les 7 articles étaient comparés à leurs
  pages cibles : 7 signalées, 223 blocs). Après : **parité 3 signalées**,
  **blocs pages 106** (contre 100 : les 6 de plus = les cartes des 7 articles
  retirés absentes des listes Ressources FR/EN — conséquence de D19).

**Héros** — fait sur **12 pages** (photo d'origine remise, aucune photo
actuelle supprimée) : accueil FR/EN, Carrières FR/EN, Découvrir FR/EN,
Ø Studio FR/EN, Azure, AWS, campagne Licences, démo Ø Bureau EN. Le 13e
(`/fr/ressources/`) **n'est pas un héros** : l'outil marque « héros » la
première photo de la page source, et sur l'ancienne page Ressources c'était
la vignette du premier article (`wp-post-image`) — la page liste n'a pas de
champ image. Rapport `blocs-manquants-pages.md` : héros 13 → **1** (celui-là).

**Lisibilité de l'accueil** avec la photo d'origine (« victrix-est-expert… »)
: vérifiée en capture à 390, 1024 et 1440 px — le voile blanc du `home-hero`
couvre la colonne de texte, rien ne casse ; axe-core vert sur `/fr/` et
`/en/`.

**Photos de l'ancien site** — fait : nouvel outil
`scripts/migration/rapatrie-images-source.py` (lit le CACHE des 173 pages, pas
le site en ligne ; écart assumé par rapport à « extract-source-page.py
--images » qui aurait refait 173 requêtes et écrit 173 fiches Markdown de
rebut). **536 images relevées, 143 déjà là, 393 téléchargées (53 Mo bruts),
0 échec.** `optimize-images.mjs --tout` (nécessaire : non référencées) ; 2 PNG
refusés par sharp (`apps-microsoft-viva*-2048x927.png` : en-tête corrompu à
la source, laissés tels quels).

**Poids** : `public/wp-content` 26 → **70 Mo** ; `dist` 69 → **106 Mo**
(**+37 Mo, au-dessus des 15 Mo** — rien retiré, comme demandé). À trancher :
garder tout dans la médiathèque, ou ne garder que les référencées (l'outil
d'optimisation liste les orphelines).

**Gate** : lint 0 err · vitest 251/251 · type-check 0 · build **189 pages**
(202 − 13 brouillons) · liens 0 cassé · sections 0 · bookshop 43/43 · aperçus
alignés · redirections 188/188 (+ `--dist`) · parité 3 (exit 0) · e2e **80
passés + 3 sautés** · blocs articles : 1 bloc (3 mots = l'ancien libellé
« Téléchargez le guide » de tendances-ti, remplacé par D18).

## Lot 4 — Forme des articles

**Fait** :

1. `docs/plan-forme-articles.md` (patrons + HTML de chacun), puis
   `src/styles/global.css` § « Forme des articles » sous `.prose` : bouton
   (`btn`/`btn-outline`, classes de `rich.ts`), encadré
   `<aside class="article-encadre">`, FAQ `<details class="article-faq">`
   (sans script, chevron CSS, focus visible), tableau
   `<div class="article-tableau">` (défilement du conteneur, en-tête,
   zébrage). La règle scoped `article-cta` de la route des articles est
   retirée.
2. **Snippets CloudCannon : NON FAIT, bloqué.** Un snippet personnalisé doit
   hériter d'un `template`, et tous les gabarits existants sont liés à un
   moteur (Hugo/Jekyll/Eleventy shortcodes, MDX, Docusaurus,
   python-markdown) — aucun n'émet du HTML dans un `.md` ordinaire (doc
   « What is a snippet », « Snippets using MDX components », schéma
   `configuration-types`). Options : (a) passer les articles en `.mdx` +
   `_snippets_imports: mdx` + 4 composants (≈ 0,5 j, risques MDX sur le
   contenu migré) ; (b) rester en `.md`, Julie colle les modèles du guide.
   Le guide décrit (b).
3. `scripts/migration/restaure-forme-articles.py` (essai = diffs, `--apply`,
   `--only`, rapport par article, refus d'écrire si le texte nu change,
   idempotent — rejeu : 0). Appliqué : **32 articles — 49 CTA, 10 encadrés,
   6 FAQ, 24 tableaux**. Les 9 questions en gras mesurées = 6 vraies FAQ
   (DORA FR/EN ×3) + 3 accroches rhétoriques isolées (« What if we told
   you… ? », ServiceNow ITSM) laissées en gras — règle : au moins 2
   questions qui se suivent. Texte nu vérifié identique à HEAD sur les 62
   articles (seuls les 4 liens de D18 diffèrent). L'icône « ampoule » des
   encadrés NIS2 (décorative) est retirée.
4. Démo : `src/data/demo/forme-articles.md` rendu par
   `src/pages/[lang]/style-guide/forme-articles.astro` sur
   `/fr/style-guide/forme-articles/` (noindex, hors sitemap, hors CMS —
   déclaré dans `tests/helpers/contenu-editable.ts`) ; axe-core **0
   violation** (10e gabarit d'`accessibilite.spec.ts`) ;
   `tests/e2e/forme-articles.spec.ts` (3 tests : rendu, FAQ sans JS au clic
   et au clavier, tableau qui défile sans déborder à 390 px).
   `docs/guide-edition.md` § « Mettre en forme un article ».

**Gate** : lint 0 err · vitest 251/251 · type-check 0 · build **190 pages** ·
liens 0 cassé · sections 0 · bookshop 43/43 · aperçus alignés · redirections
188/188 (+ `--dist`) · parité 3 (exit 0) · e2e **84 passés + 3 sautés**.

## Questions pour le matin

1. **L16** : poser `EDITOR_PREVIEW=1` sur les sites dev et Édition (UI) avant
   ou juste après le push — sinon un build sans édition visuelle.
2. **D19** : le rendu de la métadonnée `draft` (`true`/`false`) sur la carte
   CloudCannon — à regarder dans l'UI ; si illisible, retirer `metadata` et
   garder le tri.
3. **Poids** : +37 Mo dans `dist` (393 photos rapatriées). Garder tout ?
4. **Forme des articles** : snippets → `.mdx` (a) ou modèles collés (b) ?
   Et les FAQ en titres `### Question ?` (Copilot, ServiceNow, IoT) :
   dépliantes aussi ?
5. Détail préexistant vu au passage : 4 articles ont leur délimiteur de front
   matter collé au premier titre (`---## …`) — Astro les rend, mais c'est
   fragile (certification-iso, migration-windows-11, FR + EN).

## Commandes (une seule série pour la nuit)

Voir la réponse finale de la session (identiques).
