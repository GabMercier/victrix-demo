# Plan de session — ce qui reste à couvrir, contenu d'abord (2026-09-23)

> Rédigé le 2026-09-23 pendant qu'un autre agent tournait (L-grand-ecran,
> L-articles-accueil, L-cartes-chiffres) et que 5 lots du jour attendaient
> d'être commités : **rien n'a été modifié dans le code, le contenu, ni les
> autres plans.** Ce document répond à
> une seule question — « qu'est-ce qui reste, et dans quel ordre ? » — en
> mettant la **parité de contenu avec le site en ligne** en premier, catalogue
> Ø Studio compris.
>
> Il ne réécrit pas les prompts qui existent déjà : il POINTE vers eux.
> `docs/plan-livraison-finale.md` reste le plan de référence (décisions § 2,
> prompts § 4, suivi § 7) ; les prompts de lots s'y cochent, pas ici.

## 1. Où on en est (23/09, soir)

- Poussé sur `dev` : `bce519e`. **Non commité au moment d'écrire : 8 lots**
  — L14, L-seo-accueil, L-perf-images, L-contenu-perdu, L-correctifs,
  L-grand-ecran, L-articles-accueil, L-cartes-chiffres — gate complet vert sur
  l'arbre entier (chiffres dans `plan-livraison-finale.md` § 7 et dans la
  mémoire `project-status`). Cinq fichiers portent des morceaux de plusieurs
  lots (`content.config.ts`, `accueil.json`, `index.astro`, `operations.md`,
  `plan-livraison-finale.md`) : **un seul commit pour la journée**, message
  par lot. **Préalable à la prochaine session : ce commit poussé**, sinon le
  gate du lot suivant mesure aussi le travail des autres.
- Lighthouse sur le build de production : 96 / 100 / 100 / 100.
- Ce que `docs/plan-parite-et-raffinage.md` (21/09) appelait P1, P2, P3 est
  **fait sous d'autres noms** : P1 → L14 (adresses) ; P2 → L-contenu-perdu
  (Découvrir : mission, écosystème, parole d'experts, liens de la frise) ;
  P3 → L-contenu-perdu (Accueil : bandeau ISO, carrousel de 12 logos). Ce qui
  en reste est repris au § 3 ci-dessous — ne pas relancer ces prompts.

## 2. Parité de contenu : ce qui est prouvé, ce qui ne l'est pas

| Question | État | Preuve |
| --- | --- | --- |
| Chaque ADRESSE de l'ancien site mène-t-elle quelque part ? | ✅ 172 / 173 | `npm run check:old-urls` → `docs/migration/validation-301.md`, registre `docs/inventaire-pages.md` (L14) |
| Chaque redirection a-t-elle une cible qui existe ? | ✅ 175 / 175 | `npm run check:redirects -- --dist` (CI) |
| Toutes les campagnes cachées sont-elles recensées et tranchées ? | ✅ 14 pages, décisions du 21/09 | `docs/inventaire-campagnes.md` § 2 et § 6 |
| **Le TEXTE de chaque page est-il arrivé entier ?** | ❌ **jamais mesuré** | Découvrir avait perdu 3 blocs et l'Accueil 2 sans qu'aucun garde-fou ne le voie (L-contenu-perdu, 23/09). 73 expertises + 24 pages + 62 articles n'ont jamais été comparés source ↔ cible |
| **Catalogue Ø Studio** (o-studio-catalogue.victrix.ca, 16 fiches) | ❌ **0 fiche** ; 9 cartes sur 16 ; « Découvrir » mène au Contact | `docs/plan-import-catalogue-ostudio.md` — décisions prises le 18/09, rien n'est commencé |
| 3 pages `/document/*` (2 livres blancs, 1 webinaire) | 🟡 302 d'attente vers `/fr/ressources/` | L12 — décision D7 + #1633 ; PDF protégés à récupérer avec un accès admin AVANT la mise hors ligne (`docs/content-inventory.md` § 5, § 15) |
| Liste de prix Check Point FR + EN (191 SKU) | 🟡 302 d'attente vers la page de service ; données extraites | L-prix — décision D12 ; `docs/inventaire-campagnes.md` § 7 |
| FAQ d'Accompagnement IA : liens des 8 réponses | ❌ 0 lien | L08 point 4 — lister les liens de l'ancienne page pour Julie |
| Greenhouse (offres d'emploi, bas de l'ancienne page Carrières) | ❌ absent | décision Victrix (jeton) — story à créer |

Ce qui est **volontairement** abandonné (ne pas rouvrir) : vœux des fêtes,
documents O bureau protégés, 3 brouillons d'événements, `/cache/`,
`/xmlrpc.php/` — `docs/inventaire-campagnes.md` § 6, `docs/inventaire-pages.md`.

## 3. Les lots, dans l'ordre

Un lot = une session neuve (`CLAUDE.md` chargé). Estimés en jours assistés.
La colonne « Prompt » dit OÙ est le prompt à coller ; seul L-parite-texte est
écrit ici, parce qu'il n'existait nulle part.

| # | Lot | Estimé | Dépend de | Détail | Prompt |
| --- | --- | --- | --- | --- | --- |
| 1 | **L-parite-texte** — le filet : volume de texte source ↔ cible, page par page | 0,5 j | arbre commité | § 4 | **§ 4 ci-dessous** |
| 2 | **L10** — Catalogue Ø Studio A : export + `galerie` | 1 j | — | `docs/plan-import-catalogue-ostudio.md` § 3, § 4, § 6 ; compléments § 5 ci-dessous | `plan-livraison-finale.md` § 4 → L10 |
| 3 | **L11** — Catalogue Ø Studio B : fiches par sections, route, « Découvrir » | 1,5 j | L10 | idem | `plan-livraison-finale.md` § 4 → L11, puis **revue Fable R3** (§ 5 du même plan) |
| 4 | **L-restaure** — restaurer ce que L-parite-texte a révélé | 0,5 j (à réviser) | L-parite-texte tranché par Gabriel | méthode de L-contenu-perdu : `extract-source-page.py`, sections existantes, aucun champ nouveau si possible | à écrire à partir de la liste de L-parite-texte |
| 5 | **L12** — 3 pages `/document/*` à accès conditionnel | 0,5–1 j | **D7**, #1633, PDF récupérés | `plan-livraison-finale.md` § 4 → L12 (lire : 3 pages, pas 4 — `licences-microsoft-power-platform` est déjà une campagne en 301) | idem |
| 6 | **L-prix** — page de prix Check Point | 0,5–1 j | **D12** + mention de bas de page (marketing) | `docs/inventaire-campagnes.md` § 7 | `docs/plan-parite-et-raffinage.md` § 3 → « P4 (reste) » |
| 7 | **L08** — petits retours de Julie (dont les liens de la FAQ) | 0,5 j | D5 | mémoire `julie-comments-1762`, `docs/reponses-1762.md` s'il existe | `plan-livraison-finale.md` § 4 → L08 |
| 8 | **L06** — CTA de section, cartes cliquables, boutons dans le texte | 1,5 j | — | `docs/plan-icones-cta-apercus.md` § 3–4 | `plan-livraison-finale.md` § 4 → L06, puis **R1** |
| 9 | **L16** — séparer build statique / aperçu d'édition (**la prod publierait les brouillons**) | 0,5 j | — | `docs/DEPLOYMENT.md` § 6 | `plan-livraison-finale.md` § 4 → L16, puis **R4** |
| 10 | Phase 4 restante : L17, L18 (reste), L19 (reste), L21 (hreflang, sitemap), L15 (reste : `curl -I`, D17), L13, L22, L23 | ≈ 3 j | comptes, décisions | `plan-livraison-finale.md` § 4 et § 7 | idem |
| — | Glissent entre deux gros lots : L00, L02, L04 (D2), L07 (D4) | 0,5–1 j | — | | `plan-livraison-finale.md` § 4 |

**Pourquoi cet ordre.** L-parite-texte passe devant le catalogue pour la même
raison que L14 était passé devant L06 : il coûte 0,5 j, il se rejoue, et il
peut révéler des pages à restaurer — mieux vaut le savoir AVANT d'ouvrir 2,5 j
de catalogue que pendant. L10/L11 suivent parce que c'est le plus gros trou de
contenu connu et que toutes ses décisions sont prises. L12 et L-prix attendent
des humains (D7, D12, #1633, mention de prix) : on les lance dès que la
décision tombe, pas avant. L16 est le seul lot de phase 4 qui protège contre un
incident visible en production ; il ne dépend de rien.

## 4. L-parite-texte — prompt (0,5 j, peut tourner sans surveillance)

```text
Lot L-parite-texte de docs/plan-2026-09-23.md (§ 4).
But : prouver, page par page, que le TEXTE de l'ancien site est arrivé — L14
a prouvé que chaque ADRESSE arrive (172/173) ; personne n'a comparé les
volumes. Le 23/09, trois blocs entiers de Découvrir et deux de l'Accueil
manquaient sans qu'aucun garde-fou ne le voie (lot L-contenu-perdu).

Lis d'abord, ne redécouvre pas : docs/migration/correspondance-urls.json (la
carte ancienne → nouvelle adresse, décisions comprises), docs/inventaire-
pages.md (colonne « Source du contenu » = le fichier du dépôt derrière chaque
page), scripts/migration/check-parite-live.py (tables ALIAS/ROUTES, lecture du
plan de site en ligne, cache docs/migration/urls-live.csv), scripts/migration/
extract-source-page.py (parseur d'une page SOURCE : saute en-tête, pied, nav,
formulaires ; réécrit les liens vers les URL finales — RÉUTILISE-LE, ne le
réécris pas), scripts/check-old-urls.mjs (comment on lit dist/).

1. scripts/migration/check-parite-texte.py, rejouable. UNE ligne par PAGE
   CIBLE construite (pas par ancien alias) ; source = l'adresse du plan de
   site en ligne (urls-live.csv), User-Agent navigateur, réponse mise en
   cache dans docs/migration/cache-source/ pour ne pas re-télécharger à
   chaque passe (les brouillons WordPress sont abandonnés : le site en ligne
   suffit). Pour chaque paire : (a) mots de la source (contenu principal, par
   le parseur existant) ; (b) mots de la cible dans dist/ — <main> seulement,
   sans en-tête, pied, nav, bandeau de consentement ni formulaires ; (c) ratio
   cible/source ; (d) les H2/H3 de la source ABSENTS de la cible (titres
   normalisés : casse, accents, ponctuation, espaces).
2. Sortie docs/migration/parite-texte.md : tableau du pire ratio au meilleur
   — page cible, adresse source, mots source / cible, ratio, titres perdus,
   fichier du dépôt à ouvrir. Signalé si ratio < 0,7 OU ≥ 1 titre perdu. Une
   cible PLUS longue n'est pas un défaut. Sections séparées : services,
   pages, articles, campagnes. En tête : les 5 chiffres du bilan.
3. Passe à la main sur les 10 pires : pour chacune, dire si c'est (i) du
   contenu perdu à restaurer, (ii) un bloc abandonné VOLONTAIREMENT
   (témoignages, carrousel d'articles, formulaire, widget), ou (iii) un faux
   positif du parseur. Ne restaure RIEN dans ce lot : dresse la liste des
   pages à restaurer avec le bloc manquant NOMMÉ — Gabriel tranche, la
   restauration est le lot L-restaure (plan § 3, ligne 4).
4. npm run check:parite-texte — rapport seul, code de sortie 0 (même marche
   que check:old-urls : bloquant plus tard, quand les exceptions assumées
   seront écrites, dans correspondance-urls.json, à côté des autres
   décisions).
5. Ajoute la ligne L-parite-texte au tableau § 7 de
   docs/plan-livraison-finale.md, et la commande dans docs/operations.md.

Aucun changement de contenu ni de rendu. Si le port 4321 écoute, build dans
une copie isolée (docs/operations.md § 3.1). Rituel.
```

## 5. Catalogue Ø Studio — ce qui a changé depuis le plan du 18/09

À lire AVEC les prompts L10 et L11 (ils n'ont pas été réécrits) :

- **Périmètre confirmé.** Les 16 fiches deviennent des pages PAR SECTIONS dans
  la collection `solutions` (`/fr/solutions/<slug>/`), « Découvrir » du
  catalogue mène à la fiche, le formulaire `o-studio` est SUR la fiche. Julie
  (#1762) : « Ø Studio n'est PAS un produit ; les produits sont O bureau,
  Plateforme employé, Approvisionnement TI et le catalogue » — la page
  Produits (`product-hero` · `numbered-cards` · `cta`) garde donc ses cartes et
  pointe vers le catalogue ; elle ne liste pas les 16 fiches.
- **Déjà livré depuis** : sujet Contact « Une solution du catalogue » (clé
  `solution`) et champ `contactService` par fiche (18/09) ; garde-fou
  `check:prefill` (22/09) — chaque lien `/contact` d'une fiche devra porter
  `sujet=` et `expertise=` ; `scripts/optimize-images.mjs` (23/09), rejouable
  et idempotent — passer les 75 images du catalogue par lui plutôt que par la
  recette `compress-wp-images` citée dans le plan (pièges sharp : pas
  d'`effort`/`palette` en PNG, lire le fichier en mémoire avant de réécrire
  le même chemin).
- **Fonds** : le nouveau composant `galerie` prend `fond` dans la liste
  `fonds` (clairs) — pas `fonds_etendus`, réservée aux sections qui inversent
  leurs textes. Rétro-remplissage obligatoire (règle 6) : `KEYS` de
  `scripts/backfill-section-keys.mjs` puis `npm run fix:sections`.
- **Aperçus CloudCannon** : collection `solutions` par langue, URL vérifiée
  hors ligne avec `@cloudcannon/reader` (mémoire `cloudcannon-reader-url-check`).
- **Le sous-domaine** `o-studio-catalogue.victrix.ca` (16 URL) se redirige côté
  DNS/Cloudflare, pas dans `routing.json` : à lister pour L23, hors gate.
- **Humains** : `alt` des 75 images (Julie — L10 produit la liste), prix
  publics (#1634, Marjorie — fiches en `noindex` jusque-là), 2 fiches aux
  images identiques (Ø Studio confirme laquelle est fausse), traduction EN des
  16 fiches (décidé : carte EN → Contact tant qu'elles n'existent pas).

## 6. Décisions qui bloquent un lot de ce plan

Une seule source fait foi : `docs/plan-livraison-finale.md` § 2. Celles qui
comptent ici : **D7** (documents → L12), **D12** (prix éditables ou figés →
L-prix), **D5** (page Cybersécurité → L08), **D4** (logos → L07), **D2** (H1 →
L04). Hors tableau : **#1634** (prix Ø Studio → `noindex` des fiches),
**#1633** (livre blanc du client → L12), **jeton Greenhouse** (story à créer),
**domaine canonique** (bloquant go-live, documenté dans `docs/DEPLOYMENT.md`
§ 7 — décision du 23/09 : on ne change rien pour l'instant).

## 7. Premier prompt de la session — à coller dans une session neuve

**De nuit** (personne ne répond) : coller d'abord l'en-tête de nuit de
`docs/prompts-nuit-2026-09-22.md` § 2bis, avec `docs/journal-nuit-<date>.md`
comme journal, puis ce prompt à la suite ; l'étape 2 dépose alors la liste à
trancher dans le journal et la réponse finale, sans attendre.

```text
Session du [date] — reprise du plan. Lis, dans cet ordre, sans rien modifier :
la mémoire project-status, docs/plan-2026-09-23.md (ce qui reste, contenu
d'abord), puis docs/plan-livraison-finale.md § 2 (décisions) et § 7 (suivi).

0. Point d'état en 10 lignes maximum : HEAD (doit être APRÈS bce519e : les 8
   lots du 23/09 sont commités), `git status` (l'arbre doit être PROPRE ;
   s'il reste des fichiers, liste-les et continue, mais tes commandes finales
   n'ajouteront QUE les fichiers de ton lot, nommément, jamais `git add -A`),
   port 4321 (s'il écoute : copie isolée pour le build, docs/operations.md
   § 3.1), et les décisions du § 6 encore ouvertes qui touchent la session.
1. Exécute le lot 1 du § 3 : L-parite-texte, prompt au § 4 — tel quel.
2. Rituel de fin de lot (CLAUDE.md), chiffres réels. Dans la réponse finale,
   AVANT les commandes git : la liste des pages à restaurer que le rapport a
   révélées, avec le bloc manquant nommé, pour que je tranche — puis le prompt
   du lot suivant que tu recommandes (L10 si la liste est courte, L-restaure
   si elle est longue), en une phrase de justification.
```
