# Plan du 15 juillet — clore la gate, activer les fondations, préparer la démo

> Suite de `docs/spike-cloudcannon.md` (gate en cours) et de la ronde de
> fondations construite dans la nuit du 14 au 15 (formulaires, schema.org,
> redirections CMS, brouillons, éditeurs par défaut) — voir « Ce qui a été
> ajouté » ci-dessous une fois la vérification terminée.

## 1. Clore la gate CloudCannon (30–45 min)

- [ ] Reprendre où on s'est arrêté : la « page blanche » était le **Content
      Editor** (corps markdown vide — normal). Basculer sur l'**éditeur
      visuel** via les icônes en haut à droite.
- [ ] Dérouler la grille des 8 critères de `docs/spike-cloudcannon.md`
      (palette 4 sections, réordonner, « + Ajouter » dans fr|en/, type de
      champ du formulaire, aller-retour save → commit GitHub → préversion
      Cloudflare Pages).
- [ ] **Capturer la vidéo** (composer une landing → publier → URL en ligne).
- [ ] Noter palier/prix (badge « 20 » = jours d'essai restants).
- [ ] Verdict : consigner dans `docs/spike-cloudcannon.md` (colonne Constats)
      et mettre à jour le critère 1 de `docs/analyse-criteres.md` (⚠️ → ✅ si
      concluant).

## 2. Passer en revue les fondations de la nuit (avec Claude)

Construites en autonomie (approuvées dans les docs précédents), **inertes tant
que non configurées** — rien de visible sur le site public sans variables
d'environnement :

- **Pipeline formulaires** (critère 6) : rendu réel dérrière
  `PUBLIC_FORMS_ENABLED`, endpoint `/api/forms` (validation, pot de miel,
  Turnstile optionnel, envoi SMTP2GO ou mode démo), pages `/fr|en/merci`,
  doc `docs/formulaires.md`.
- **Schema.org** (critère 7) : Organization sur toutes les pages, BlogPosting
  sur les articles, section FAQ (nouvelle 5ᵉ section de la palette) avec
  FAQPage.
- **Redirections gérées au CMS** (critère 8) : `src/data/redirects.json`
  éditable dans CloudCannon (« Redirections »), validé au build, émis dans
  `dist/_redirects`.
- **Brouillons d'articles** (critère 2) : interrupteur « Brouillon » — visibles
  dans l'aperçu CloudCannon, exclus du site public.
- **Éditeurs par défaut** : Campagnes/Accueil/Expertises s'ouvrent dans
  l'éditeur visuel (fini le piège du Content Editor vide).

- [ ] Revue rapide du diff ensemble + commit sur `spike/cloudcannon`.

## 3. Décisions à prendre (l'ordre importe peu, mais avant la démo équipe)

| # | Décision | Contexte |
|---|---|---|
| 1 | **Conversion Bookshop de l'accueil** | Le seul écart restant du critère 1 (l'accueil s'édite par panneau de données, pas clic-sur-composant). ~1 jour. Go/no-go selon le verdict de la gate. |
| 2 | **Provisionner les secrets formulaires** | Réactiver le compte SMTP2GO (clé API), créer le widget Turnstile dans le tableau de bord Cloudflare (site key + secret). À poser dans : CF Pages (prod), CloudCannon (préversion), `.env` local. Liste exacte dans `docs/formulaires.md`. |
| 3 | **CSP pour Turnstile** | `public/_headers` est gelé jusqu'ici; l'activation de Turnstile exige `script-src`/`frame-src` `https://challenges.cloudflare.com`. Claude l'applique sur demande. |
| 4 | ~~**Maquettes**~~ **TRANCHÉ (14 juil.)** | **Fonctionnel d'abord** : v1 complète sur le design actuel, refonte graphique appliquée plus tard comme « re-peau » via les design tokens. Plus aucun jalon n'est bloqué par le design. |
| 5 | **Retrait de Sveltia** | Phase 2 du plan pivot — seulement après verdict de gate positif. `public/admin/`, bloc CSP `/admin`, worker OAuth. |
| 6 | **Bandeau de consentement (Loi 25) + Clarity** | Requis avant tout script analytique (critère 9). Besoin d'un choix de design — pas commencé exprès. |
| 7 | **Collection Services (6 services du cahier des charges)** | Structure prête à répliquer (pattern expertises), mais le gabarit mérite l'intrant maquettes. Planifier, pas précipiter. |

## 4. Préparer la démo équipe (les sceptiques)

- [ ] Vidéo de la gate (point 1) + scénario : composer une landing devant eux,
      publier, montrer l'URL de préversion partageable non indexée.
- [ ] Messages clés : `docs/analyse-criteres.md` (arguments) + le tableau des
      critères 1–10 avec leur état réel démontrable.
- [ ] Chiffres à jour : estimation révisée 26–34 j (+4–7 j éditeur visuel),
      licences CloudCannon vs les 8 licences WordPress du cahier des charges.

## Rappels

- Personne ne committe à la place de l'utilisateur — la ronde de nuit est dans
  l'arbre de travail, à committer après revue (point 2).
- Le dépôt de production « propre » sera semé depuis la branche gagnante à la
  fin du spike — pas de rebuild à neuf (voir `docs/plan-pivot-editeur.md`).
